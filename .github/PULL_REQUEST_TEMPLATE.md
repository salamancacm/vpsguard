## What does this change

## Why

## How was this tested

- [ ] `gofmt -l .` is clean
- [ ] `go vet ./...` is clean
- [ ] Tested against a real container/VM (not just compiled) — describe how:

## Checklist (for new/changed checks or remediations)

- [ ] Remediation is idempotent (safe to run repeatedly)
- [ ] Remediation backs up any file it modifies (`internal/harden.BackupFile`)
- [ ] README's check table updated if applicable
